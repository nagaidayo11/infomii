# Logos

LPの実績カードで使うロゴファイル置き場です。  
ベース名が一致していれば、拡張子は自動判定されます（`svg -> png -> webp -> jpg`）。

- `city-business-hotel.*`
- `resort-spa-hotel.*`
- `station-town-hotel.*`

推奨仕様:
- 形式: `svg` 推奨（`png/webp/jpg` でも可）
- 目安サイズ: `220x56` 以上
- 背景: 透過推奨
