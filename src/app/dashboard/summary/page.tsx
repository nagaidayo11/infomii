import { InfomiiDashboard } from "@/components/dashboard";

/**
 * モダンSaaSダッシュボード（統計 + ページ一覧）
 * メインの /dashboard と併用可能なシンプルビュー
 */
export default function DashboardSummaryPage() {
  return <InfomiiDashboard />;
}
